execution:
  - concurrency: 100
    ramp-up: 1m
    hold-for: 5m
    scenario: quick-test

scenarios:
  quick-test:
    requests:
      - http://blazedemo.com

reporting:
  - final-stats
  - console

settings:
  artifacts-dir: ./artifacts
